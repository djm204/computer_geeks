%h2 Shopping Cart
= debug params
= session[:cart].count unless session[:cart].nil?
- if flash[:notice]
  .notice= flash[:notice]
- if @products_in_cart.nil? == false
  = paginate @products_in_cart
  = render partial: 'shared/product',
    collection: @products_in_cart, locals:{partial_name:'cart_product'}
  = paginate @products_in_cart
- else
  %h3 There are no products in your cart!
