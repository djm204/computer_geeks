!!!
%html
  %head
    %title Computer Geeks
    = stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true
    = javascript_include_tag "application", "data-turbolinks-track" => true
    = csrf_meta_tags
  %body#body
    %header#head
      %nav
        %ul
          - get_Nav_Items.each do |name,url|
            - if name == "Cart"
              %li.nav_right{class: is_active(request.original_url,url[1,url.length], 'active')}= link_to_unless_current(name, url)
            - elsif user_signed_in? && name == "My Account"
              %li.nav_right{class: is_active(request.original_url,url[1,url.length],'active')}= link_to_unless_current(name, url)
            - elsif name != "My Account"
              %li{class: is_active(request.original_url,url[1,url.length], 'active')}= link_to_unless_current(name, url)
        %h2 Computer Geeks            
        -	if user_signed_in?
          %div.signed_in_welcome= 'Welcome Back, ' + current_user.name
          %div.login
            = link_to "My Account", "account"
            = "|"
            = link_to "Sign out", destroy_user_session_path, :method => :delete
        -	else
          %div.login= link_to('Login', new_user_session_path)
        - if notice
          %div.alert.alert-success= notice
        - if alert
          %div.alert.alert-danger= alert
    %section#container
      = yield
    %p footer